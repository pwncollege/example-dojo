name: Update Dojo

on: push

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: pwncollege/dojo-update
        with:
          dojo: example
          update_code: ${{ secrets.UPDATE_CODE }}
